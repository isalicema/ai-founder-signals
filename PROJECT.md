# AI Founder Signals

> **本文件是这个项目进度与决策的权威来源。** 隔一阵子回来、或换人接手，先读这份。
> 最后更新：2026-08-31（Feed 改为北京时间当天晨报）

---

## 这是什么

一个**只有一个用户**（Alice）的私人 feed：每天早上自动抓好全球 AI 创始人的一手访谈，
判定、摘要、分档，打开 `localhost:8166` 三十秒扫完，挑出值得深看的。

**它不是内容站，是 `collection-manager` 的前置漏斗。**

```
🤖 自动探测（17 个信源）  →  📰 Feed 扫一遍  →  🔖 标「深看」
                                                    ↓
                          Obsidian 收藏夹  ←  collection-manager 深度分析
```

深度分析那条链路已经跑了两年，不重做。这个项目只解决「**发现**」——
把 Alice 从「偶然刷到才知道」变成「每天早上摆在那儿」。

**长期北极星**：100 位关键创始人 × 500 场访谈 × 一套可追溯的观点结构。
但 MVP 只做 feed，其余（人物页、主题页、观点库、月报）全部推迟，见 §6。

---

## 1. 现在能用吗

**能。** 每天 06:00 自动跑，`localhost:8166` 随时可看。

| | |
|---|---|
| 测试 | 176 全绿 |
| 信源 | **17 个全部可跑**（YouTube 8 · 播客 3 · JSON API 3 · HTML 3） |
| 部署 | 本地 launchd（不是 GitHub Actions，原因见 §4.6） |
| 成本 | **约 $1/月**（DeepSeek）+ Supabase 免费档 |

```bash
scripts/afs status   # 服务 + 数据概况
scripts/afs run      # 立刻跑一次，不等 06:00
scripts/afs open     # 打开 feed
scripts/afs build    # 改完代码后重建网页
```

### 信源现状

| 类型 | 数量 | 状态 |
|---|---|---|
| YouTube | 8 | ✅ 可跑 |
| 播客（小宇宙） | 3 | ✅ 可跑（用 show notes，见 §4.5） |
| HTML（晚点 / 品玩 / AI 闹） | 3 | ✅ 可跑；11 / 38 / 4 个标题锚真源冒烟通过 |
| JSON API（Founder Park / 暗涌 / Z Potentials） | 3 | ✅ 可跑；6 / 20 / 20 条真源冒烟通过 |

---

## 2. 一天里发生什么

```
06:00  launchd 调起 worker
         ├─ discover  每个信源拉列表，只要「上次检查之后」的新内容
         └─ process   逐条：
              ① 准入三层漏斗（信源先验 → 标题规则 → LLM 兜底）
              ├─ 不通过 → 入库，tier=folded，不抓正文、不花摘要钱
              └─ 通过   → 抓正文 → 摘要 → 实体登记 → 分档 → 入库
随时   打开 localhost:8166 扫当天 feed，点「🔖 深看」标记
       页面只读北京时间今天捕捉入库的内容；历史留库但不混进晨报
稍后   对妙蛙种子说「处理收藏队列」→ collection-manager → Obsidian
```

---

## 3. 技术栈

```
Next.js 15 + React 19       前台 feed（单页）
Supabase Postgres           5 张表：source / item / entity / feedback / job
                            全表 RLS 拒绝一切（anon key 是公开的）
DeepSeek v4 Flash           判定 + 摘要，供应商可换（AFS_LLM_PROVIDER）
Postgres 表当任务队列        SKIP LOCKED，没引 Redis
launchd                     每天 06:00 + 网页常驻
```

**故意没有的东西**：Redis、独立向量库、对象存储、审核后台、用户系统。
理由都在架构文档里，别顺手加回来。

---

## 4. 关键决策（**改动前先读这里**）

这些不是随便定的，每条背后都有一次真实的翻车或实测。

### 4.1 不做人物白名单

原设计里「命中 20 人白名单」是分档打分的第二大项。**取消了**，两个理由：
Alice 列不出来（大量是没听过的初创公司创始人），更重要的是**它反目标**——
这个产品的价值就在于发现你还不认识的人，按「认识的人加分」等于把探测器变成回音室。

`entity` 表因此从**输入**变成**产出**：系统自动登记遇到的每个人和公司，
Alice 事后给感兴趣的打星标（权重只有 0.15，故意压低，不能把新面孔挤下去）。

### 4.2 准入标准不判断「是不是 AI 公司」

首次真实冒烟时，`Supabase: Cash Does Not Equal Success` 被判 false，
理由「Supabase 是数据库公司，不属于 AI 公司」。**模型没判错，是规则错了**——
「算不算 AI 公司」越到边缘越不稳定（Cursor？Vercel？Notion？）。

改为「科技创业公司的创始人」，并明确要求不裁定公司身份。
**领域过滤由信源选择承担，不由 LLM 承担**——跟 4.1 是同一个道理：
把模型不擅长的身份裁定挪出判定环节。

⚠️ 立项书 §2.3 仍写「AI 公司」，与此不一致，待 RabbitT 同步。

### 4.3 体裁判定与身份判定是正交两维

标题里的「对谈」只能证明**这是一场访谈**，证明不了**受访者是创始人**。
RabbitT 的评测样本一击命中：「对话 Gartner 分析师」体裁强命中但受访者是分析师。

所以 `verdict`（体裁）喂给打分，`requiresLlm`（身份存疑）剥夺 L1 的独自放行权。
**强正向不再跳过 L2。** 省钱的大头在负向判定侧（融资/榜单直接判负、不调 LLM），那里没变。

### 4.4 摘要里禁止出现引号原话，且由代码守着

系统**不保存原文**（阅后即焚），模型一旦编一句原话，事后没有任何办法核验。
prompt 里写了禁止，但 prompt 是请求不是保证——所以 `guards.ts` 检出长引号 →
重试一次 → 仍违规就**直接摘掉引文内容**。宁可摘要少一句，也不留假原话。

### 4.5 播客用 show notes，但标注来源

单集页 `__NEXT_DATA__` 里内嵌完整 show notes（一集约 2800 字，含嘉宾背景和
带时间戳的 OUTLINE）——对我们的用途甚至比逐字稿更好：已经结构化、没有口语废话。

但 show notes 是**节目方撰写的说明，不是对话实录**。所以 `EphemeralContent.provenance`
标注来源，摘要器据此收紧措辞：不写成受访者第一人称，写不出具体判断就如实概括议题范围。

> [!warning] 2026-08-31 更正
> 早先这里写的是「小宇宙不提供逐字稿」，**这个说法是错的**。
> 页面数据里有 `transcript` 和 `transcriptMediaId` 字段——小宇宙**有**文稿功能，
> 只是正文在需要登录的接口后面（`api.xiaoyuzhoufm.com/v1/transcript/get` → 401，
> `transcript.xyzcdn.net` → 403）。
>
> 所以准确说法是：**有文稿，但要登录，我们不取**——理由同 §4.7，
> 跟 36氪的 `sign`、知乎的 `x-zse-96` 是同一堵墙。
> 别让后来人看到 `transcript` 字段以为是我们漏做了。

### 4.6 用本地 launchd，不用 GitHub Actions

实测 **YouTube 拦截数据中心 IP**：Actions runner 上 yt-dlp 报
`Sign in to confirm you're not a bot`，一条字幕都取不到。11 个可跑信源里 8 个是 YouTube，
云端跑出来只剩标题卡——「Mac 没开也能跑」换来的是个降级 feed。

云端其余部分是通的（DB 经 Session pooler、DeepSeek HTTP 200），
`worker.yml` 保留手动触发，定时停用。

### 4.7 反爬对抗一律不做

已经对三个信源说过不：**36氪**（接口要 `sign` 签名）、**知乎**（要 `x-zse-96` 签名头）、
**YouTube cookies**（绕 bot 检测）。硅星人最后走**品玩自有站**这个正门解决了。

⚠️ **不要因为技术上做得到就破例。** 这条线一破就没了。

### 4.8 健康检查必须 `--noproxy`

Alice 常开 Clash，环境里有 `HTTP_PROXY=127.0.0.1:7897`。curl 会把**对 localhost 的
健康检查也走代理**，结果永远探测失败——服务明明是好的。

实测踩过两次，第一次误判成「启动慢」查了很久。所有内部探测都带 `--noproxy '*'`。

### 4.9 切片折叠与角标计数共用同一套聚类判据

Lex 这类频道会把一场访谈切成 5 条分两天发。切片的字幕内容各不相同，
simhash 去重从原理上挡不住，只能在展示层聚类：
**同信源 + 至少共享一个人物 + 发布时间相差 3 天内 = 同一场**。

两处都用这个判据，避免规则打架：
- `data.ts` 的密集受访角标——数「对话」不数「条目」，一场被切五段不算密集发声
- `model.ts` 的 `groupConversations`——feed 里折成一组，展开才看各段

代表条目取**段落最长**的那条（切片里内容最全的一段最能说明这场讲了什么）。
「值得先看的 N 场」也跟着数场，否则会出现「说 9 条、只看到 5 张卡」的错位。

### 4.10 晚点的证书链要补一张根证书

晚点 LatePost 的证书由 Let's Encrypt 2025 年的新中间证书 `YR1` 签发，其根
`ISRG Root YR` 还没进 Node 24 的 CA 列表，也不在 macOS 钥匙串里。
Node 直接 `UNABLE_TO_GET_ISSUER_CERT_LOCALLY`，而 curl 能通（会自动补链）——
**这类「curl 行、代码不行」的差异很容易被误判成反爬**。

解法是 `NODE_EXTRA_CA_CERTS` 指向交叉签名版本（由 Node 已信任的 ISRG Root X1
签发），校验全程开着。⚠️ **不要用 `NODE_TLS_REJECT_UNAUTHORIZED=0` 或按域名放行**
——那才是真妥协，而且会掩盖将来真实的证书问题。

`tools/chkCerts.ts` 用来判断这份证书还需不需要；Node 收录新根后就能删。

### 4.11 不做反馈调权，打分改用 purity + 内容长度

Alice 看完首批 50 条后的判断：**不点赞不点踩、不调信源权重**，信源已经少而精，
只看卡片本身质量。这作废了 v1.1 §5.2 那套「反馈闭环」——我当时称它为 MVP 的核心机制，
一天真实使用就证伪了。

连带发现打分公式已经坏掉：第一大项 `source.weight` 本来是设计给反馈学习的，
不做反馈它就恒为 1.0，**实测最高分只到 0.50 而高亮门槛 0.65——高亮档从未触发过**。

改成：
```
0.35 × source.purity      手工设定的信源质量，本来就是质量判断却一直没进公式
0.25 × 标题信号
0.20 × 判定置信度
0.20 × 内容充实度          min(字数/20000, 1)，区分完整深访与正片切片
```

**高亮改为「当天前 3 场」而非绝对分数**：绝对门槛会「有的天 0 条、有的天 20 条」，
相对排名每天都有意义。实测改完排序立刻合理——5 万字完整访谈浮顶，1 千字切片沉底。

因为没有 rescore，手动设的 tier 永远不会被覆盖，`user_signal` 列也就不需要了。

### 4.12 被拒条目的判定置信度不能当正分

`admission_confidence` 在**被拒**时表示「确信它**不是**创始人访谈」，是拒绝的把握程度。
当质量分加进打分，等于让「判得最准的拒绝」拿最高分——实测把
「对谈英伟达研究副总裁」「对谈投资人汪天凡」两条顶进了高亮。

`handlers.ts` 被拒分支传 `admissionConfidence: 0`；`pickHighlights` 另外还要
**先排除 `tier='folded'`**，光看分数不够。两条都有回归测试，测试名里写着触发它们的真实条目。

### 4.13 永不丢弃，只降权

判定不通过的条目**仍然入库**，只是 `tier='folded'` 折叠到底部。
判定必然有误判，丢掉不可逆、折叠可逆。**手动设的 tier 也永远不会被覆盖**——
因为没有 rescore（见 §4.11）。


### 4.14 网页进程共用一条数据库连接池

原先 `loadFeed` / `applyFeedAction` 每次请求都 create → query → close。
数据库在新加坡、还要走代理出去，每次请求重新做 TCP + TLS 握手——
**实测首字节 23.7 秒**。改成 `src/db/shared.ts` 的共用连接池后降到 0.65-0.98 秒。

⚠️ 别在这两个地方 `close()` 连接——连接池是长驻的，关掉下次又要重连。
worker 那边是短命进程，仍然自己建自己关，不共用。

### 4.15 Feed 是当天晨报，不是历史仓库

真实使用时页面出现 **109 条**，恰好是上线三天的累计：8 月 29 日 50 条、
30 日 43 条、31 日 16 条。根因是 `loadFeed()` 只按 `first_seen_at` 倒序取最近
120 条，没有日期条件；主卡、低分抽屉、筛选选项和统计因此全部混入历史。

现在 Feed 统一使用北京时间 `[今天 00:00, 明天 00:00)` 的左闭右开窗口，
按 `item.first_seen_at` 过滤。**不用 `published_at`**：一篇旧访谈今天才被系统发现，
仍然属于今天的捕捉结果。历史条目继续完整留在数据库，只是不再显示在当天 Feed。

这条边界在数据库查询层执行，因此顶部统计、筛选、「全部已阅」、高亮候选和低分抽屉
天然共享同一批当天数据。唯一继续读取近 30 天历史的是「🔥 本月第 N 场」角标计数，
它只提供密集发声背景，不会把历史卡片带回页面。

2026-08-31 实库验收：查询返回 **16 条，其中 folded 12 条**；页面「全部信号」显示 16。
北京时间零点边界有独立回归测试。

⚠️ **这个边界的代价，用之前先知道**：隔一天不打开，那天的条目就再也不出现在
feed 里（数据仍在库中，只是没有入口）。配合「全部标为已阅」，未读的东西会静默消失。
目前接受——Alice 的用法就是每天早上扫一次。**如果出现「周末回来发现什么都没有」
的不适，应该加「昨天／前天」的切换，而不是把窗口无脑放宽**——放宽就退回到
109 条混杂历史的老问题。

⚠️ **空状态必须区分两种「空」**：今天确实没有新信号 vs 筛选把它们排除了。
原文案「清除一两个筛选」是为后者写的，安静的一天或 worker 没跑时会把人引向
错误方向。现在前者会提示 `afs run` / `afs logs`。

---

## 5. 已知问题

| | 严重度 | 说明 |
|---|---|---|
| **M6 按钮那半边没真跑过** | 🟠 | 队列 → `collection-manager` → 归档已验通；但「◇ 深看」按钮 → 写库这一段我验不了（浏览器面板始终 hidden，卡片入场动画不启动、按钮尺寸 0×0）。**代码读下来是对的，等 Alice 真点一次确认** |
| 摘要重试率约 48% | 🟠 | DeepSeek 结构化输出不稳，成本因此翻倍。已加正文上限和 2 次重试缓解，未根治 |
| 卡片入场动画是「可见性开关」 | 🟡 | `animation-fill-mode: both` + 0% 关键帧 opacity 0 —— 动画一旦没启动，卡片**永久不可见**。正常使用碰不到，但属于「一个 CSS 属性决定内容可不可见」的脆弱写法。建议改成只做位移不做 opacity |
| feed 首屏 chrome 偏多 | 🟡 | 看到第一张卡前约 1.5 屏。UI 方向由 Alice 与 kimi work 另行实验 |
| `dead_link` 状态 UI 未处理 | 🟢 | schema 支持，界面没做 |

---

## 6. 明确推迟的

写下来是为了以后不用重新讨论。**立项书 §4.2-§4.5、§6、§7 描述的是 Phase 2/3，
MVP 只实现 §4.1 的首页卡片流。**

访谈详情页 · 人物页 · 主题页 · Insight/Quote 观点库 · 引文回指校验 ·
跨载体去重合并 · 语义搜索 · 月报 · ASR 转写无字幕视频 · 转公开内容站

其中**语义搜索需要重新引入素材留存**（当前是阅后即焚），属于架构回滚不是渐进迁移。

### 这几个是「评估过、决定不做」，不是「还没做」

| | 为什么不做 |
|---|---|
| **M7 认证** | 本地 launchd 只监听 127.0.0.1，外网访问不到，加登录是自我妨碍。数据库那层 RLS 已挡住公网路径。转公开站时再上 |
| **M8 信源管理页** | 17 个源基本不动，改一次写条 SQL 就够。为它做个后台是过度建设 |
| **反馈调权 cron / rescore** | 见 §4.11。`feedback` 表继续只写不读，保留 `opened_source`/`archive_requested` 自动埋点备查 |

---

## 7. 下一步：不是写代码，是用

功能上 MVP 齐了，已经进入日常使用。接下来继续验证：**连用 5 天，
每天打开只看到当天新东西、30 秒扫完**。前面那些技术验收都过了，这条只有 Alice 能验。

每天起床后 `afs open`，重点看三件事：

1. **有没有当天内容** —— worker 增量窗口只取「上次检查之后」的新内容，
   Feed 再按北京时间当天的 `first_seen_at` 展示；
   17 个信源一夜下来应该有几条到十几条。一条没有就是哪里断了，看 `afs logs`。
2. **高亮那 3 场值不值得点开** —— 打分公式刚改（§4.11），
   排序对不对只能靠真实内容判断。
3. **点一条「◇ 深看」** —— 这是整条链路唯一没被真实走通的一段（见 §5）。
   点完对妙蛙种子说「处理收藏队列」。

⚠️ 别急着改功能。这一轮里被证伪的（反馈闭环）和被发现坏掉的（高亮档从未触发），
都是纸上推演看不出来、用了才知道的。

---

## 8. 分工

| | |
|---|---|
| **Alice** | 产品判断、内容品味、信源选择、最终裁决 |
| **妙蛙种子** | 架构、准入判定、LLM 层、部署、GitHub 提交把关 |
| **星子** | M1-M5 + `JsonApiAdapter` + `HtmlAdapter` 已交付；17 个信源均有适配器 |
| **RabbitT** | 立项书、主题体系、评测样本；待同步 §4.2 的准入标准变更 |

---

## 9. 相关文档

| | |
|---|---|
| `docs/HANDOFF.md` | 给星子的工程交接：接口、红线、部署踩坑 |
| `docs/SPEC-JsonApiAdapter.md` | 已完成的通用 JSON API 适配器（3 个真源配置） |
| `docs/SPEC-HtmlAdapter.md` | 已完成的通用 HTML 适配器（3 个真源 + 真实页面 fixture） |
| `.github/workflows/diagnose.yml` | 云端环境体检——本地测不出的问题跑它，别猜 |
| 架构文档 v2.1 | `~/Smart Workspace/Machiwhale Studio/Project/` |
| 立项书 | 同上（RabbitT 维护） |
